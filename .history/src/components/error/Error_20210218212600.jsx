import React from 'react';

const createHeaderSlideStyles = createUseStyles(() => ({

export const Error = () => {
    return (
        <div className={classNames.error}>
            {'ERROR'}
        </div>
    );
}